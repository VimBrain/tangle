name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up C
      run: sudo apt-get install build-essential
    - name: Build Project
      run: make all
